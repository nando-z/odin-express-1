{
  "name": "express",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "serve": "bun run --hot --watch index.ts --bun"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@types/express": "^5.0.1",
    "dotenv": "^16.5.0",
    "express": "^5.1.0"
  }
}
